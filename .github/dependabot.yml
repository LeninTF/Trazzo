version: 2
updates:
  # 1. Configuración para el Backend (Java / Maven)
  - package-ecosystem: "maven"
    directory: "/back" # Asegúrate de que esta ruta apunte a la carpeta de tu backend
    schedule:
      interval: "weekly"
      day: "monday"
    groups:
      # Agrupará todas las dependencias de Spring Boot en un solo Pull Request
      spring-boot-updates:
        patterns:
          - "org.springframework.boot:*"
      # Agrupará cualquier otra actualización de Maven en otro Pull Request
      all-other-maven:
        patterns:
          - "*"

  # 2. Configuración para el Frontend (Angular / pnpm)
  - package-ecosystem: "npm" # CRÍTICO: Se usa 'npm' aunque uses pnpm
    directory: "/front" # Asegúrate de que esta ruta apunte a la carpeta de tu frontend
    schedule:
      interval: "weekly"
      day: "monday"
    groups:
      # Agrupará todas las dependencias del ecosistema Angular en un solo PR
      angular-updates:
        patterns:
          - "@angular/*"
      # Agrupará el resto de librerías del frontend
      all-other-frontend:
        patterns:
          - "*"

  # 3. Configuración para el Middleware (C# / .NET) TODO IMPLEMENTAR EN CONJUNTO CON T-MIDDLEWARE
#  - package-ecosystem: "nuget"
#    directory: "/middleware"
#    schedule:
#      interval: "weekly"
#      day: "monday"
#    groups:
#      dotnet-updates:
#        patterns:
#          - "*"

  # 4. Configuración para los propios Workflows de GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    groups:
      actions-updates:
        patterns:
          - "*"