name: Seguridad - Escáner de Secretos (Gitleaks)

on:
  pull_request:
    branches: [ "master" ]
  push:
    branches: [ "master" ]

permissions:
  contents: read         # Leer código e historial git
  security-events: write # Subir resultados SARIF a GitHub Code Scanning

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    name: Buscar credenciales expuestas
    steps:
      - name: Checkout del código
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Crítico para que escanee todo el árbol de git

      - name: Ejecutar Gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}