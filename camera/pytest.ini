[pytest]
testpaths = tests
pythonpath = src
asyncio_mode = auto
addopts = -ra --strict-markers --tb=short --basetemp=.pytest_tmp
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
    ignore::UserWarning
